# 版本维护：每次更新此技能时，必须同步提升下方 display_name 中的版本号。
interface:
  display_name: "GM 分层多代理配置 V0.1.6"
  short_description: "配置 Sol 指挥与 Luna 执行的分层多代理工作流"
  default_prompt: "使用 $sol-luna-setup 为当前项目配置并验证 Sol-Luna 分层代理。"

policy:
  allow_implicit_invocation: true
